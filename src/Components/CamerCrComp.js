import React from 'react'
import { Outlet } from 'react-router-dom'

const CamerCrComp = () => {
    return (
        <div>
            <Outlet></Outlet>
        </div>
    )
}

export default CamerCrComp
